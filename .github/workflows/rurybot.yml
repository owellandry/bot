name: Notificación a Discord

on:
  push:
    branches:
      - main

jobs:
  notificar:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar mensaje a Discord
        env:
          WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          curl -H "Content-Type: application/json" \
               -X POST \
               -d "{\"content\": \"🚀 Nuevo commit en *${{ github.repository }}* por *${{ github.actor }}*. Mensaje del commit: '${{ github.event.head_commit.message }}'\"}" \
               $WEBHOOK_URL
